export const createCorn = () => {
    let seed = {
        type: "Corn",
        height: 180,
        output: 6
    }
    return [seed,seed]
}

// JavaScript code for isArray() function
// function func() {
// 	console.log(Array.isArray(seed));
// }

// func();
